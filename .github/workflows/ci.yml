name: CI

on:
  pull_request:
    paths:
      - 'src/**'
      - 'docs/**'
      - 'blog/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/ci.yml'
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'docs/**'
      - 'blog/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Link checker runtime smoke
        run: npx --no-install markdown-link-check --version

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
